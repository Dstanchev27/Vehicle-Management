@page
@model AccessDeniedModel
@{
    ViewData["Title"] = "Access denied";
}

<div class="card">
    <div class="card-header table-dark text-dark fw-semibold text-danger">
        @ViewData["Title"]
    </div>
    <div class="card-body">
        <p class="text-danger">You do not have access to this resource.</p>
    </div>
</div>
